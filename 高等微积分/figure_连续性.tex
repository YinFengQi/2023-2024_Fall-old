\documentclass{standalone}
\usepackage{tikz}

\begin{document}

\tikzset{every picture/.style={line width=0.75pt}} %set default line width to 0.75pt        

\begin{tikzpicture}[x=.75pt,y=.75pt, scale=1]

\path (0,150) -- (0,-150);

\def\dis{300}

\draw (0,0) [rotate around={-30:(0,0)}]ellipse (85 and 130);
\draw (0,0) [rotate around={-60:(0,0)}]ellipse (40 and 90);

\draw (\dis,0) [rotate around={-30:(\dis,0)}]ellipse (85 and 130);
\draw (\dis,0) [rotate around={-60:(\dis,0)}]ellipse (30 and 70);

\filldraw (0,0)    circle (2pt)
    coordinate (x0)
    node[anchor=north east]{$x_0$};

\filldraw (\dis,0) circle (2pt) 
    node (fx0) {}
    node[anchor=north] {$f(x_0)$};

\draw[->] (x0) to [bend left=30](fx0);

\node at (-30, -100) {$X$};
\node at (\dis-30, -100) {$Y$};

\node at (-40, -40) {$f^{-1}(V)$};
\node at (\dis-40, -30) {$V$};

\end{tikzpicture}
\end{document}